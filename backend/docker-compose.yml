name: tannerpagos

volumes:
  pgdata:

services:
  postgres:
    image: postgres:17
    container_name: tanner-postgres
    environment:
      POSTGRES_USER: tannerpagos
      POSTGRES_PASSWORD: QQ5d8q3aF6kj
      POSTGRES_DB: tannerpagos
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
